<div class="cards">
<% @articles.each do |article| %>
<div class="card">
<h2><%= link_to article.title, article_path(article), class: "title" %></h2>
<p><%= article.content.first(100) %></p>
</div>
<% end %>
</div>
